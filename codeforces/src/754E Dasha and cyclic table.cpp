//
// Created by Psy.C on 2026/9/1.
//
/**
- q-'a' ：把字符转成 0~25 的索引（'a'→0, 'b'→1, ..., 'z'→25）
- p[i][字符索引][列号 j] = 1 ：标记第 i 行第 j 列是这个字符
快速查询"第 i 行哪些列是字符 c"
初始假设 所有位置都匹配 ，后面遇到不匹配的就置 0。
遍历模式矩阵的 r 行 c 列
- 逗号表达式先读入 q
- q != '?' ： 只有非通配符才处理 （ '?' 可以匹配任意字符，不需要约束 ans）
&= 意味着： 只有当原来 ans 是 1 且新条件也为 1 时，结果才是 1 。
初始 ans 全 1，每遇到一个模式矩阵中的确定字符，就用它来 过滤掉不满足的位置 。
p[k][q-'a']  →  文本第 k 行中，字符 q 出现的列位置（bitset）
j%m          →  模式列 j 对齐到文本的哪一列（循环）
>> j%m       →  右移 j%m 位：文本列 col 的字符，现在放到列 col-j%m
<< m-j%m     →  左移 m-j%m 位：处理循环环绕的情况
|            →  两者取或，合并两种情况
当模式第 j 列是字符 q 时，文本第 k 行的第 col 列要匹配，需要文本中 位置 col + j%m 处 是字符 q （循环）。
通过移位，我们把"文本中 q 出现的位置" 对齐到 "模式中 j 列约束的位置"。

读入 + 构建 p O(n×m) 逐字符读入
处理模式 O(r×c×n×N/64) 每个模式位置遍历 n 行，每行 bitset 操作是 O(N/64) 位并行
输出 O(n×m) 逐位输出

这是在做 循环移位的批量位置对齐 。对于文本第 k 行的每一列 col，要判断 text[k][col + j%m] == q （循环模 m）。
bitset 移位把所有列的判断 一次并行完成 。
- >> j%m ：处理不跨边界的情况
- << m-j%m ：处理跨边界环绕的情况
- | ：合并两种情况
### 2. 行索引映射 (k+n-i%n)%n
模式垂直循环，模式第 i 行 ≡ 模式第 i%r 行，但代码里用了 n （文本行数）。这实际上是在做 循环对齐 ：模式的第 i 行约束文本的某一行，
通过这个模运算把"模式行号"映射到"文本行号对 ans 的贡献位置"。
### 3. &= 的逐步过滤
初始 ans 全 1（假设全部匹配），每遇到一个确定字符就用 AND 过滤。这是 交集思想 ：最终 ans 是所有模式位置约束的交集。
### 4. scanf(" %c", &q) 中的空格
" %c" 中的空格会让 scanf 跳过所有空白字符 （换行、空格等）再读字符，避免读到换行使程序出错。
### 5. bitset<N> 的大小
N=423，所以每个 bitset 有 423 位。这保证了移位时不会丢失有效位（因为 m ≤ N，移位量 < N）。
### 6. 时间复杂度为何可接受
常规二维模式匹配是 O(r×c×n×m) = O(423×423×423×423) ≈ 320 亿次，这会超时。用 bitset 后，每行的 m=423 次判断
被压缩成 423/64 ≈ 7 次位运算，实际复杂度降了约 60 倍。
 */
#include <bits/stdc++.h>
#define rep(i,n) for (int i=0; i<n; ++i)
using namespace std;
constexpr int N = 423, M = 26;

char q;
bitset<N> p[N][M], ans[N];
int main() {
    int n, m; cin >> n >> m;
    rep(i, n) rep(j, m) scanf(" %c", &q), p[i][q-'a'][j] = ans[i][j] = 1;
    int r, c; cin >> r >> c;
    rep(i, r) rep(j, c) if (scanf(" %c", &q), q != '?')
        rep(k, n) ans[(k+n-i%n)%n] &= p[k][q-'a']>>j%m|p[k][q-'a']<<m-j%m;
    rep(i, n) rep(j, m) printf("%d%s", (int)ans[i][j], j==m-1?"\n":"");
    return 0;
}