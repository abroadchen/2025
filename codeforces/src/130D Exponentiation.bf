//
// Created by Psy.C on 2025/11/18.
//
/*
& - 读取第一个数字（设为a）并压入栈中
00p - 将栈顶值存储到坐标(0,0)的位置
& - 读取第二个数字（设为b）并压入栈中
& - 读取第三个数字（设为n）并压入栈中
01p - 将栈顶值存储到坐标(0,1)的位置
1 - 将数字1压入栈中（初始化结果为1）
> - 指令指针向右移动（进入主循环）

00g - 从坐标(0,0)获取存储的值a
* - 将当前结果乘以a
01g - 从坐标(0,1)获取存储的值n
% - 对b取模（result = (result * a) % b）
\ - 交换栈顶两个元素（将n移到栈顶）
1- - 将n减1
: - 复制栈顶元素
#v_ - 条件垂直跳转：
如果n不等于0，继续向下执行
如果n等于0，跳过向下移动
$ - 弹出并丢弃栈顶元素
. - 输出最终结果
@ - 结束程序

^ - 指令指针向上移动（回到第一行继续循环）
< - 指令指针向左移动

计算 (a^n) mod b
*/


&00p&&01p1>00g*01g% \1-:#v_$.@
          ^             \<