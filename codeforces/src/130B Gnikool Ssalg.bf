//
// Created by Psy.C on 2025/11/18.
//
/*

> - 指令指针向右移动
~ - 从输入读取一个字符并压入栈中
: - 复制栈顶字符
64+ - 将6和4压入栈，然后相加得到10（换行符的ASCII码）
- - 计算读取字符与10的差值（即字符ASCII值减去10）
#v_ - 条件跳转：
如果差值为0（即读到换行符），则向下移动到第二行
如果差值非0，则继续向右执行
$ - 弹出并丢弃栈顶元素（换行符）
>:#,_@ - 向右移动，重复打印栈中字符直到栈空后结束程序
第二行：^ <

^ - 指令指针向上移动（回到第一行）
中间的空格不影响执行
< - 指令指针向左移动

栈的特性来实现字符串反转
*/
>~:64+-#v_$>:#,_@
^       <