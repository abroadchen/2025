//
// Created by Psy.C on 2025/12/22.
//
/*
*f[2][N][N][4]：四维DP数组，用于状态转移
ans = 0：答案计数器
g[N][N]：辅助数组
 *
 *外层循环，i从1到n
*n0 = i & 1：当前层索引（0或1）
n1 = (i + 1) & 1：下一层索引（0或1）
这是滚动数组优化技巧
 *
*对于所有j,k组合，增加f[n0][j][k][0]的值
累加所有状态到答案ans中
 *计算前缀和到辅助数组g中
 *反向更新f数组的第一维状态
 *更新f数组的第二维状态
 *更新f数组的第三维状态
 *计算辅助数组g的新值
 *计算二维前缀和
 *更新f数组的第四维状态
 *
*n0：当前处理的行在DP数组中的索引（0或1）
n1：下一行在DP数组中的索引
 *f[layer][j][k][t]
*layer：当前处理的层数（滚动数组优化）
j,k：某种区间的左右端点
t：状态类型（0-3表示四种不同的状态）
 *
*g[j-1][k]：前一行相同列范围的累积值
f[n0][j][k][1]：当前位置的第1种状态值
f[n0][j-1][k][0]：当前位置前一列的第0种状态值
*纵向累积：g[j-1][k] 从前一行继承
当前贡献：f[n0][j][k][1] 当前行的新贡献
状态转移：f[n0][j-1][k][0] 从之前状态的转移
 *
*f[n1][j][k-1][1]：到前一列为止的累积和
g[j][k]：当前列的贡献值
结果：到当前列为止的总累积和
 *
*1. g[j][k+1] - 右侧继承
从右侧已经计算好的值继承信息
2. f[n0][j][k][2] - 当前状态贡献
当前位置第2种状态的贡献
3. f[n0][j][k+1][0] - 扩展状态影响
向右扩展一位的第0种状态影响
*外层反向遍历列：从右到左
内层反向遍历行起点：从大到小
先计算右侧信息：利用g[j][k+1]
再更新左侧状态：通过f[n1][j+1][k][2]
 *
*1. f[n0][j][k][3]
当前区域：行j到行k，第3种状态
2. f[n0][j-1][k][2]
上方区域：行(j-1)到行k，第2种状态
3. f[n0][j][k+1][1]
右侧区域：行j到行(k+1)，第1种状态
4. f[n0][j-1][k+1][0]
左上区域：行(j-1)到行(k+1)，第0种状态
 *
*当前关注区域 [j,k]:
+-------+-------+-------+
|       |       |       |
|  A    |   B   |   C   |  ← (j-1)行
|       |       |       |
+-------+-------+-------+
|       |███████|       |
|  D    |███████|   E   |  ← j行到k行（当前区域）
|       |███████|       |
+-------+-------+-------+
|       |       |       |
|  F    |   G   |   H   |  ← (k+1)行
|       |       |       |
+-------+-------+-------+
*f[n0][j][k][3]：区域E（当前核心区域）
f[n0][j-1][k][2]：区域B+D（上方区域）
f[n0][j][k+1][1]：区域E+G（下方扩展）
f[n0][j-1][k+1][0]：区域B+D+E+G（整体扩展）
 *
*g[j-1][k] = 上方已计算的值，用于正向遍历时的纵向累积
g[j][k+1] = 右侧已计算的值，用于反向遍历时的横向累积
 *
 */
#include <iostream>
#define fast ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0)
#define ll long long
#define rep(i,n) for(int i=1;i<=n;++i)
using namespace std;

constexpr int N = 155, mod = 1000000007;
ll add(ll& a) { return a > mod ? a %= mod : a; }

int main() {
    fast;
    int n, m; cin>>n>>m;
    ll f[2][N][N][4], ans = 0, g[N][N];
    rep(i,n) {
        const int n0 = i & 1, n1 = (i + 1) & 1;
        rep(j,m) for (int k = j; k <= m; ++k) {
            add(++f[n0][j][k][0]);
            for (int t = 0; t <= 3; ++t) add(ans += f[n0][j][k][t]);
        }
        rep(j,m) for (int k = j; k <= m; ++k) add(g[j][k] = g[j][k-1]+f[n0][j][k][0]);
        for (int k = m; k >= 1; --k) for (int j = k; j >= 1; --j) {
            f[n1][j][k][0] = add(g[j][k] += g[j+1][k]);
        }
        rep(j,m) {
            for (int k = j; k <= m; ++k) add(g[j][k] = g[j-1][k]+f[n0][j][k][1]+f[n0][j-1][k][0]);
            for (int k = j; k <= m; ++k) add(f[n1][j][k][1] = f[n1][j][k-1][1] + g[j][k]);
        }
        for (int k = m; k >= 1; --k) {
            //第一层：计算g[j][k]值
            for (int j = k; j >= 1; --j) add(g[j][k]=g[j][k+1]+f[n0][j][k][2]+f[n0][j][k+1][0]);
            //第二层：更新f[n1][j][k][2]状态
            for (int j = k; j >= 1; --j) f[n1][j][k][2] = (f[n1][j+1][k][2]+g[j][k])%mod;
        }
        rep(j,m) for (int k = j; k <= m; ++k) {
            add(g[j][k] = f[n0][j][k][3] + f[n0][j-1][k][2] + f[n0][j][k+1][1] + f[n0][j-1][k+1][0]);
        }
        rep(j,m) for (int k = m; k >= j; --k) add(g[j][k] += g[j][k+1]);
        rep(j,m) for (int k = j; k <= m; ++k) add(g[j][k] += g[j-1][k]);
        rep(j,m) for (int k = j; k <= m; ++k) f[n1][j][k][3] = g[j][k];
    }
    cout << ans << '\n';
    return 0;
}