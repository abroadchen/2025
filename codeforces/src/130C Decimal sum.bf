//
// Created by Psy.C on 2025/11/18.
//
/*
第一行：v v <

第一个v - 指令指针向下移动
第二个v - 指令指针向下移动（在第二行的相应位置）
< - 指令指针向左移动（在第二行的末尾）

第二行：>0&>\&+\1-:|

> - 指令指针向右移动（程序起点）
0 - 将数字0压入栈中（初始化累加器为0）
& - 读取第一个数字n（要计算前n个自然数的和）
> - 继续向右移动
\ - 交换栈顶两个元素（将n移到栈顶）
& - 读取下一个数字（但实际上这里应该是进入循环）
+ - 将两个数字相加
\ - 交换栈顶两个元素
1- - 将栈顶数字减1
: - 复制栈顶元素
| - 条件垂直跳转：
如果栈顶元素非0，指针向下移动
如果栈顶元素为0，指针向上移动



第三行： >$.@

> - 指令指针向右移动
$ - 弹出并丢弃栈顶元素（丢弃最后的0）
. - 输出栈顶的累加结果
@ - 结束程序

计算 1 + 2 + 3 + ... + n = n×(n+1)/2
*/
v  v       <
>0&>\&+\1-:|
           >$.@