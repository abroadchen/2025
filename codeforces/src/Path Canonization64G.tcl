//
// Created by Psy.C on 2025/10/29.
//
/*
 *从标准输入读取一行文本，存储到变量s中
*使用正则表达式循环替换路径中的/./或/.$为/
这是规范化路径，移除当前目录引用
regsub返回替换发生的次数，当次数>0时继续循环
例如：/home/./user → /home/user
 *
*循环处理路径中的/../（上级目录引用）
匹配模式：/[任意字母数字或点组成的目录名]/../
将整个模式替换为单个/
例如：/home/user/../ → /home/
这个循环会处理嵌套的情况，直到所有/../都被处理完
 *
*移除路径末尾的斜杠（如果存在）
例如：/home/user/ → /home/user
 *
*如果处理后的路径为空，则设置为根目录/
这处理了类似a/../这样的情况
 *
*检查路径是否以/../或/..开头（试图访问根目录的上级）
如果是这种情况，输出-1表示无效路径
否则输出规范化后的路径
 */
gets stdin s
while {[regsub {/\.(/|$)} $s / s]} {}
while {[regsub {/[[\w\.]+/\.\.(/|$)} $s {/} s]} {}
regsub /$ $s {} s
if {$s == ""} {set s /}
if {[regexp {^/\.\.(/|$)} $s]} {puts -1} {puts $s}