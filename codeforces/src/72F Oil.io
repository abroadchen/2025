//
// Created by Psy.C on 2025/11/3.
//
/*
定义一个名为g的方法，接受两个参数：x和r
这个方法用于计算某种连通分量的数量
将字符串r按空白字符分割成列表q
移除列表的第一个元素（通常是节点数量等元数据）
创建一个空列表k
遍历q中的每个元素v，将其转换为数字并添加到k中
对k中的数字进行排序，结果存储在p中
初始化z为排序后列表p的大小加1
z表示初始的连通分量数量
遍历排序后的列表p，i是索引，v是值
如果当前值v等于1，则减少一个连通分量（1是特殊的起始节点）
如果当前值v等于参数x，则减少一个连通分量（x是目标节点）
如果不是第一个元素，检查当前元素是否与前一个元素连续
如果p[i-1] + 1 == p[i]，说明这两个节点是连续的，可以合并为一个连通分量，因此减少一个连通分量
结束循环并返回连通分量数量z

从标准输入读取所有行，存储在列表s中
将第一行分割成单词，存储在列表a中

调用g方法两次：
第一次：参数为a[0]和s[1]（第一行的第一个数字和第二行）
第二次：参数为a[1]和s[2]（第一行的第二个数字和第三行）
将两次调用的结果相乘并输出

*/
g := method(x, r,
  q := r split
  q removeFirst
  k := list()
  q foreach(v, k append(v asNumber))
  p := k sort
  z := p size + 1
  p foreach(i, v,
    if(v == 1, z = z - 1)
    if(v == x asNumber, z = z - 1)
    if(i > 0, 
      if(p at(i - 1) + 1 == v, z = z - 1)
    )
  )
  return z
)

s := File standardInput readLines
a := s at(0) split
(g(a at(0), s at(1)) * g(a at(1), s at(2))) println
